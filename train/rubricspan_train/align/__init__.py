"""align —— extracted_span 字符级对齐后处理（M1）。

职责（技术方案 §7.3 第 4 步）：
- 将大模型输出的 extracted_span 映射回学生答案的精确字符级起止位置；
- 三级匹配策略：精确匹配 → 去标点模糊匹配 → 最长公共子串兜底；
- 输出对齐成功率统计（出口标准 > 90%）；
- 对齐失败样本按策略转为"无答案"负样本或丢弃。

本模块是单元测试重点：三级匹配策略均须有边界用例覆盖。
"""
