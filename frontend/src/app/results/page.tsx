/**
 * 结果与统计（M5 实现）：评分结果导出（Excel/JSON）+ 统计看板（分数分布、得分点命中率）。
 */
export default function ResultsPage() {
  return (
    <main className="mx-auto max-w-4xl px-6 py-8">
      <h1 className="text-2xl font-bold">结果与统计</h1>
      <p className="mt-2 text-gray-500">骨架占位：M5 阶段实现（执行计划 §4.6）。</p>
    </main>
  );
}
