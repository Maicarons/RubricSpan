/**
 * 答卷上传（M5 实现）：文本单份/批量导入 + 试卷图片上传（触发 OCR）+ OCR 预览。
 */
export default function AnswersPage() {
  return (
    <main className="mx-auto max-w-4xl px-6 py-8">
      <h1 className="text-2xl font-bold">答卷上传</h1>
      <p className="mt-2 text-gray-500">骨架占位：M5 阶段实现（执行计划 §4.6）。</p>
    </main>
  );
}
